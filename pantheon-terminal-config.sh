gsettings set org.pantheon.terminal.settings unsafe-paste-alert false
gsettings set org.pantheon.terminal.settings save-exited-tabs false
gsettings set org.pantheon.terminal.settings remember-tabs false
gsettings set org.pantheon.terminal.settings cursor-color '#FF3300'
gsettings set org.pantheon.terminal.settings font 'Ubuntu Mono Regular 12'
gsettings set org.pantheon.terminal.settings background 'rgba(0, 0, 0, 1)'
gsettings set org.pantheon.terminal.settings follow-last-tab true

gsettings get org.pantheon.terminal.settings encoding
gsettings get org.pantheon.terminal.settings foreground
gsettings get org.pantheon.terminal.settings palette
gsettings get org.pantheon.terminal.settings unsafe-paste-alert
gsettings get org.pantheon.terminal.settings tab-bar-behavior
gsettings get org.pantheon.terminal.settings cursor-shape
gsettings get org.pantheon.terminal.settings background
gsettings get org.pantheon.terminal.settings natural-copy-paste
gsettings get org.pantheon.terminal.settings font
gsettings get org.pantheon.terminal.settings allow-bold
gsettings get org.pantheon.terminal.settings remember-tabs
gsettings get org.pantheon.terminal.settings alt-changes-tab
gsettings get org.pantheon.terminal.settings cursor-color
gsettings get org.pantheon.terminal.settings save-exited-tabs
gsettings get org.pantheon.terminal.settings follow-last-tab
gsettings get org.pantheon.terminal.settings audible-bell
gsettings get org.pantheon.terminal.settings shell
gsettings get org.pantheon.terminal.settings scrollback-lines

